# AG-UI Server Configuration

# Azure OpenAI Configuration (Option 1 - Preferred)
AZURE_AI_PROJECT_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o-mini

# Azure Credential Configuration
# These are required when running in environments with multiple managed identities (e.g., AKS)
# For local development, these can be left empty to use DefaultAzureCredential
# AZURE_CLIENT_ID=your-managed-identity-client-id
# AZURE_TENANT_ID=your-tenant-id

# Optional: If not using DefaultAzureCredential (for local development only)
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# OpenAI Configuration (Option 2 - Fallback)
# OPENAI_API_KEY=your-openai-api-key

# AG-UI Client Configuration
AGUI_SERVER_URL=http://127.0.0.1:5100/
