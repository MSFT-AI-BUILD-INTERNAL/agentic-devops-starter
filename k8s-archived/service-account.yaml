---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentic-devops-sa
  namespace: default
  annotations:
    # Client ID will be substituted by envsubst in the GitHub Actions workflow
    azure.workload.identity/client-id: "${AZURE_CLIENT_ID}"
  labels:
    azure.workload.identity/use: "true"
