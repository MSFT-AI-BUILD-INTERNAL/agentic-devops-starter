apiVersion: v1
kind: Service
metadata:
  name: agentic-devops-service
  labels:
    app: agentic-devops
spec:
  type: LoadBalancer
  selector:
    app: agentic-devops
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5100
    name: http
